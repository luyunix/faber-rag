[runner]
# 禁用文件监控器，避免扫描 transformers 库时产生警告
# 注意：watcherType 在新版 Streamlit 中已移除，使用 fileWatcherType 替代
fileWatcherType = "none"

[server]
# 禁用静态文件服务
enableStaticServing = false

# 隐藏警告信息
headless = true
