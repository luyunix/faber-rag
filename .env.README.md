# API Key 配置说明

## 使用环境变量管理 API Key

为了安全起见，本项目使用 `.env` 文件来管理敏感的 API Key 信息，避免将其提交到代码仓库。

## 使用步骤

### 1. 创建 .env 文件

复制示例文件创建你的 `.env` 文件：

```bash
cp .env.example .env
```

### 2. 填写 API Key

编辑 `.env` 文件，填写你的 DashScope API Key：

```bash
# Qwen (通义千问) API Key - DashScope
DASHSCOPE_API_KEY=sk-your-actual-api-key-here
```

### 3. 自动加载

项目会自动从 `.env` 文件加载环境变量到配置中。`config/settings.yaml` 中的 API Key 配置使用 `${DASHSCOPE_API_KEY}` 占位符。

## 配置说明

### settings.yaml 中的环境变量语法

```yaml
llm:
  provider: "qwen"
  model: "qwen-plus"
  base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
  api_key: "${DASHSCOPE_API_KEY}"  # 从环境变量加载
  
embedding:
  provider: "qwen"
  model: "text-embedding-v3"
  base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
  api_key: "${DASHSCOPE_API_KEY}"  # 从环境变量加载
  
vision_llm:
  provider: "qwen"
  model: "qwen-vl-plus"
  base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
  api_key: "${DASHSCOPE_API_KEY}"  # 从环境变量加载
```

### 支持的环境变量格式

- `${VAR_NAME}` - 标准格式
- 如果环境变量未设置，将保留原始占位符

## 获取 DashScope API Key

1. 访问 [阿里云 DashScope 控制台](https://dashscope.console.aliyun.com/)
2. 注册/登录阿里云账号
3. 开通 DashScope 服务
4. 创建 API Key
5. 将 API Key 复制到 `.env` 文件中

## 安全提示

- ⚠️ **不要** 将 `.env` 文件提交到 Git
- ✅ `.env` 已在 `.gitignore` 中
- ✅ 提交的是 `.env.example` 示例文件
- 🔒 妥善保管你的 API Key

## 故障排查

### 问题：配置加载失败

**检查项：**
1. 确认 `.env` 文件存在于项目根目录
2. 确认 `DASHSCOPE_API_KEY` 已正确设置
3. 检查是否安装了 `python-dotenv` 依赖

### 手动设置环境变量（可选）

如果不想使用 `.env` 文件，可以在终端中手动设置：

```bash
export DASHSCOPE_API_KEY=sk-your-api-key-here
```

## 依赖

项目需要 `python-dotenv` 来加载 `.env` 文件：

```toml
[project]
dependencies = [
    "python-dotenv>=1.0.0",
]
```
