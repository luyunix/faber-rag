# 更新日志

## 2026-07-10

### 新增

- 新增可复用的后端 `QueryService`，统一 API 与查询工作流的组装逻辑。
- 完成 Custom 评估，支持 `expected_chunk_ids` 和 `expected_sources` 两种正确答案标签。
- 新增 Custom 数据集校验、空检索结果评分，以及 Hit Rate 和 MRR 汇总。
- 新增 Custom 评估与 Golden Test Set 解析的后端单元测试。
- 为架构总览、文档处理、检索流程、评估系统和设计原则新增零基础讲解。
- 新增 TF-IDF、BM25、余弦相似度、RRF、重排序和评估打分的完整计算示例。
- 评估面板新增 Custom 数据集模板和前端 Golden Test Set 校验。

### 变更

- 将系统原理标签页和大型说明区拆分为职责清晰的独立组件文件。
- 提升文档摄取、上传清理、追踪存储、配置缓存和集合兼容性。
- 改进 Ragas 对 OpenAI 兼容服务的初始化，并提供更明确的依赖错误信息。
- 改进评估 API 对格式错误或缺少字段的数据集的校验和错误响应。
- 更新评估面板，清楚说明 Custom 与 Ragas 的适用场景。

### 修复

- 修复评估报告序列化时读取错误查询数量字段的问题。
- 修复 Custom 在检索结果为空时返回空指标的问题。
- 修复 `expected_sources` 已读取但未参与评分的问题。
- 修复文档处理部分失败后仍可能报告成功的问题。
- 修复不同 Chroma 版本下的集合列表和向量存储兼容问题。

### 验证

- Custom 评估后端测试共 7 项，全部通过。
- 后端源码和测试代码编译通过。
- Custom 评估 API 对合法数据集返回 `200`，对缺少标签的数据集返回 `400` 并指出题号。
- 前端 TypeScript 检查和生产构建通过。
- 已在浏览器中验证系统原理各标签页和 Custom 评估流程，包括窄屏布局。
